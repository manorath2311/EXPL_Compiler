type
markList
{
    str name;
    int avg;
}
endtype

decl
    markList m1,m2,m3;
enddecl

int main()
{
    decl
        markList man1,man2;
    enddecl
    begin
        read(man1.name);
        read(man1.avg);

        if(man1.avg == 20) then 
          write("yes");
        else
          write("no");
        endif;
    
    return 0;
    end
}